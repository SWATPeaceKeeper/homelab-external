# ============================================================================
# Homelab External - Environment Variables
# ============================================================================
# Wird automatisch von bootstrap.sh generiert.
# Manuelle Erstellung: cp .env.example .env && nano .env
# ============================================================================

# DOMAIN
DOMAIN=robinwerner.net
SUBDOMAIN_PREFIX=homelab-external

# TRAEFIK - Dashboard Login
# Generieren: htpasswd -nb admin DEINPASSWORT
# WICHTIG: Jedes $ muss mit $$ escaped werden!
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$...

# POSTGRESQL (shared by Headscale + Healthchecks)
# Generieren: openssl rand -hex 16
POSTGRES_PASSWORD=

# HEADSCALE / HEADPLANE
# Wird von headscale-setup.sh automatisch gesetzt
HEADSCALE_API_KEY=

# TAILSCALE
# Pre-Auth Key: docker exec headscale headscale preauthkeys create --user homelab --expiration 24h
TS_AUTHKEY=

# HEALTHCHECKS
# Generieren: openssl rand -hex 25
HEALTHCHECKS_SECRET=
HEALTHCHECKS_SMTP_PASSWORD=

# ALLGEMEIN
TZ=Europe/Berlin
