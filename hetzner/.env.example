# ============================================================================
# Homelab External - Environment Variables
# ============================================================================
# Kopiere diese Datei zu .env und fülle die Werte aus:
# cp .env.example .env
# nano .env
# ============================================================================

# -----------------------------------------------------------------------------
# DOMAIN KONFIGURATION
# -----------------------------------------------------------------------------
# Deine Domain (z.B. robinwerner.net)
DOMAIN=robinwerner.net

# Subdomain-Prefix (z.B. homelab-external → uptime.homelab-external.robinwerner.net)
SUBDOMAIN_PREFIX=homelab-external

# -----------------------------------------------------------------------------
# TRAEFIK
# -----------------------------------------------------------------------------
# Dashboard Login (generieren mit: htpasswd -nb admin DEINPASSWORT)
# Das Ergebnis sieht so aus: admin:$apr1$...
# WICHTIG: Jedes $ Zeichen MUSS mit $$ escaped werden!
# Beispiel: admin:$apr1$xyz → admin:$$apr1$$xyz
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$ruca84Hq$$mbjdMZBAG.KWn7vfN/SNK/

# -----------------------------------------------------------------------------
# POSTGRESQL (für Headscale)
# -----------------------------------------------------------------------------
# Datenbank-Passwort für Headscale PostgreSQL
# Generieren mit: openssl rand -hex 16
# WICHTIG: Muss auch in headscale/config.yaml eingetragen werden!
POSTGRES_PASSWORD=

# -----------------------------------------------------------------------------
# HEADSCALE / HEADPLANE
# -----------------------------------------------------------------------------
# API Key für Headplane (generieren nach Headscale-Start):
# docker exec headscale headscale apikeys create --expiration 365d
HEADSCALE_API_KEY=

# Cookie Secret für Headplane (MUSS exakt 32 Zeichen sein!)
# Generieren mit: openssl rand -hex 16
COOKIE_SECRET=

# -----------------------------------------------------------------------------
# TAILSCALE
# -----------------------------------------------------------------------------
# Pre-Auth Key für Tailscale Client (generieren nach Headscale-Setup):
# docker exec headscale headscale preauthkeys create --user homelab --expiration 24h
TS_AUTHKEY=

# -----------------------------------------------------------------------------
# HEALTHCHECKS
# -----------------------------------------------------------------------------
# Secret Key für Healthchecks (generieren mit: openssl rand -hex 25)
HEALTHCHECKS_SECRET=

HEALTHCHECKS_SMTP_PASSWORD=

# -----------------------------------------------------------------------------
# ALLGEMEIN
# -----------------------------------------------------------------------------
# Timezone
TZ=Europe/Berlin
